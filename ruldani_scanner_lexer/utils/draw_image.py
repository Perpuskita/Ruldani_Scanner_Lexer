# import manim


class draw_image:
    def __init__(self, length: int, width: int, nama):
        self.base = self.draw_base(name=nama, length=length, width=width)

    def render(self, tabel ):
        # ambil circle
        # tentukan apakah accepting node
        # jika ya gunakan draw accepting
        # jika tidak gunakan circle
        # cetak circle di titik x urutan
        return None

    def draw_circle(self, name: str, radius: float, x: int, y: int):
        # panggil base 
        # print nama di x dan y pada radius
        # print circle sebesar radius
        return None 

    def draw_accepting(self, nama: str):
        # panggil base
        # print nama
        # print double circle
        return None

    def draw_base(self, name: str, length: int, width: int):
        # draw base awal
        # draw 
        return None

    def draw_line(self, panjang: int, lebar: int, x: int, y: int):
        return None

if __name__ == "__main__" :
    print("oke")

